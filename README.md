Para executar um container a partir da imagem do desafio, basta executar o comando:

```docker run edivaldorsj/desafio-golang-img:latest```

Será impresso no terminal a seguinte mensagem: ```Code.education Rocks!```